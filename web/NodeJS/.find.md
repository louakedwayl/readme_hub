# 🧠 Cours JavaScript : La méthode `.find()`

## 1️⃣ Introduction

En JavaScript, la méthode `.find()` est utilisée sur les **tableaux** pour **retrouver le premier élément** qui satisfait une condition donnée. La condition est définie via une **fonction callback**.

Syntaxe générale :

```js
array.find(callback(element, index, array))
```

* `callback` : fonction qui teste chaque élément.
* `element` : élément courant du tableau.
* `index` (optionnel) : position de l’élément dans le tableau.
* `array` (optionnel) : le tableau sur lequel `.find()` est appelé.

---

## 2️⃣ Exemple de base

```js
const pokemons = [
  { id: 1, name: "Bulbizarre" },
  { id: 2, name: "Herbizarre" },
  { id: 3, name: "Florizarre" }
];

const id = 2;
const pokemon = pokemons.find(pokemon => pokemon.id === id);
console.log(pokemon); // { id: 2, name: 'Herbizarre' }
```

* `.find()` **parcourt le tableau** automatiquement.
* Le **callback** reçoit chaque élément en argument (`pokemon` ici).
* Dès que le callback retourne `true`, `.find()` renvoie **cet élément** et **s’arrête**.
* Si aucun élément ne correspond, `.find()` renvoie `undefined`.

---

## 3️⃣ Explication détaillée

1. `pokemons.find(...)` → on appelle `.find()` sur le tableau.
2. `pokemon => pokemon.id === id` → fonction fléchée **callback**.
3. Chaque élément du tableau est **passé automatiquement** à la fonction comme argument.
4. `.find()` retourne **le premier élément** pour lequel la fonction retourne `true`.

### Version classique sans flèche

```js
const pokemon = pokemons.find(function(pokemon) {
  return pokemon.id === id;
});
```

---

## 4️⃣ Points importants

* **Argument du callback** : nom libre (`pokemon`, `current`, `p`...) mais représente **l’élément courant**.
* **Retour du callback** : `true` → élément trouvé, `false` → continue.
* **Type** : attention, comparer correctement (`===`) entre nombre et string.

  ```js
  const idNum = parseInt(req.params.id, 10);
  const pokemon = pokemons.find(p => p.id === idNum);
  ```
* `.find()` **s’arrête dès qu’il trouve** l’élément correspondant.

---

## 5️⃣ Exemple avec `console.log` pour visualiser

```js
const pokemons = [
  { id: 1, name: "Bulbizarre" },
  { id: 2, name: "Herbizarre" },
  { id: 3, name: "Florizarre" }
];

const id = 2;

const pokemon = pokemons.find(pokemon => {
  console.log("Élément courant :", pokemon);
  return pokemon.id === id;
});

console.log("Résultat :", pokemon);
```

Console :

```
Élément courant : { id: 1, name: 'Bulbizarre' }
Élément courant : { id: 2, name: 'Herbizarre' }
Résultat : { id: 2, name: 'Herbizarre' }
```

---

## 6️⃣ Bonus : plusieurs paramètres dans le callback

```js
array.find((element, index, array) => { ... });
```

* `index` : position de l’élément dans le tableau.
* `array` : le tableau complet, parfois utile pour des comparaisons plus complexes.

---

## 7️⃣ Résumé

| Élément   | Description                                                                |
| --------- | -------------------------------------------------------------------------- |
| `.find()` | Recherche **le premier élément** correspondant à la condition              |
| Callback  | Fonction passée à `.find()` pour tester chaque élément                     |
| Argument  | Chaque élément du tableau est passé automatiquement à la fonction callback |
| Retour    | Le **premier élément trouvé** ou `undefined` si aucun n’est trouvé         |

💡 **Astuce** : `.find()` est idéal pour chercher un objet dans un tableau d’objets par une propriété spécifique (comme `id`).
